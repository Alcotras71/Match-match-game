const initialState = {
  friends: [
    { id: 1, name: 'Andrey' },
    { id: 2, name: 'Saga' },
    { id: 3, name: 'Redux' },
  ],
};

const sidebarReducer = (state = initialState) => {
  return state;
};

export default sidebarReducer;
